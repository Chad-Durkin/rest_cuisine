@{
    Layout = "Shared/_Layout.cshtml";
}

<form action="/cuisines" method="post">
    <div class="row">
        <div class="col s12">
            <h5>Add Cuisine</h5>
            <input type="text" name="cuisine" class="validate" placeholder="New Cuisine">
        </div>
    </div>
    <button class="btn red" type="submit" name="button">Add Cuisine</button>
</form>
